<Window x:Class="NekoChat.App.AlertBoxWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:conv="clr-namespace:NekoChat.App.Converters"
        mc:Ignorable="d"
        Title="NekoChat Alert Box"
        Width="500" Height="360"
        MinWidth="200" MinHeight="150"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize"
        Topmost="True"
        ShowInTaskbar="True"
        WindowStartupLocation="Manual">
    <Window.Resources>
        <conv:KindToIconConverter x:Key="KindToIconConverter" />
        <conv:KindToSeparatorConverter x:Key="KindToSeparatorConverter" />
        <DropShadowEffect x:Key="TextGlow" Color="Black" BlurRadius="6" ShadowDepth="0" Opacity="0.95" />
    </Window.Resources>

    <Grid x:Name="RootGrid" Background="Transparent" MouseEnter="RootGrid_MouseEnter" MouseLeave="RootGrid_MouseLeave">
        <!-- Idle-invisible content: nothing renders here on stream until an alert fires -->
        <StackPanel x:Name="AlertContent" Visibility="Collapsed" HorizontalAlignment="Center" VerticalAlignment="Center">
            <Image x:Name="AlertImage" MaxWidth="460" MaxHeight="260" Stretch="Uniform" />
            <TextBlock HorizontalAlignment="Center" Margin="0,12,0,0" FontSize="22" FontWeight="Bold"
                       Foreground="White" TextWrapping="Wrap" TextAlignment="Center" MaxWidth="460"
                       Effect="{StaticResource TextGlow}">
                <Run Text="{Binding Kind, Converter={StaticResource KindToIconConverter}, Mode=OneWay}" />
                <Run Text="{Binding AuthorName, Mode=OneWay}" />
                <Run Text="{Binding Kind, Converter={StaticResource KindToSeparatorConverter}, Mode=OneWay}" />
                <Run Text="{Binding Body, Mode=OneWay}" />
            </TextBlock>
        </StackPanel>

        <!-- Positioning chrome: invisible until the mouse hovers this window, so it
             never shows up on stream — only needed while setting up the OBS source. -->
        <Border x:Name="DragHandle" Background="#77000000" CornerRadius="4" Padding="8,4"
                HorizontalAlignment="Left" VerticalAlignment="Top" Opacity="0"
                MouseLeftButtonDown="DragHandle_MouseLeftButtonDown">
            <TextBlock Text="Alert Box — drag to move" Foreground="White" FontSize="11" FontWeight="Bold" />
        </Border>
        <Rectangle x:Name="ResizeGrip" Width="18" Height="18" HorizontalAlignment="Right" VerticalAlignment="Bottom"
                   Fill="#77000000" Opacity="0" Cursor="SizeNWSE"
                   MouseLeftButtonDown="ResizeGrip_MouseLeftButtonDown"
                   MouseLeftButtonUp="ResizeGrip_MouseLeftButtonUp"
                   MouseMove="ResizeGrip_MouseMove" />
    </Grid>
</Window>
